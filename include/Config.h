#ifndef TEVES_CONFIG_H
#define TEVES_CONFIG_H

#define APP_WINDOW_WIDTH 300
#define APP_WINDOW_HEIGHT 300
#include <stdint.h> 
#define u8 uint8_t
#define u16 uint16_t
#define i16 int16_t
#define MAX(a, b) ((a) > (b) ? (a) : (b))

#endif